{
  "name": "ai-trading-platform",
  "version": "2.0.0",
  "description": "AI Trading Platform - Gratis Selamanya. Trading Cerdas untuk Semua.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && netlify deploy --prod --dir=dist"
  },
  "dependencies": {
    "i18next": "^23.10.1",
    "i18next-browser-languagedetector": "^7.2.0"
  },
  "devDependencies": {
    "vite": "^5.2.0",
    "terser": "^5.30.0"
  }
}